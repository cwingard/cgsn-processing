config: !include testing_config.yaml
meta:
    name: &mooring "{{ config['meta']['name'] }}"
    array: 'Coastal Pioneer MAB'
    category: 'Surface Mooring'
    subcategory: 'High Power'
    disposition: "{{ config['meta']['disposition'] }}"
    deployment: "{{ config['meta']['deployment'] }}"
    deploy_name: "{{ config['meta']['deployment_name'] }}"
    deploy_cruise: "{{ config['meta']['deploy_cruise'] }}"
    start_date: "{{ config['meta']['start_date'] }}"
    latitude: "{{ config['meta']['latitude'] }}"
    longitude: "{{ config['meta']['longitude'] }}"
    watch_circle: "{{ config['meta']['watch_circle'] }}"
    water_depth: &site_depth "{{ config['meta']['water_depth'] }}"
    stop_date: "{{ config['meta']['stop_date'] }}"
    recover_cruise: "{{ config['meta']['recover_cruise'] }}"
paths:
    raw: "{{ config['paths']['raw'] }}"
    parsed: "{{ config['paths']['parsed'] }}"
    processed: "{{ config['paths']['processed'] }}"
